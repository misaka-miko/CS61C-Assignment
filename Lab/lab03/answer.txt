1. What do the .data, .word, .text directives mean (i.e. what do you use them for)? Hint: think about the 4 sections of memory.

2. Run the program to completion. What number did the program output? What does this number represent?

3. At what address is n stored in memory? Hint: Look at the contents of the registers.

4. Without actually editing the code (i.e. without going into the “Editor” tab), have the program calculate the 13th fib number (0-indexed) by manually modifying the value of a register. You may find it helpful to first step through the code. If you prefer to look at decimal values, change the “Display Settings” option at the bottom.

1. '.data' directives tell the assembler that the following is not instruction, but the data stored in memory. '.word' directives are used in '.data' to reserve 4 bytes for a word and initialize it with a value. '.text' is the section for storing the program (instructions).

2. The program outputs 34. It represents the 9th (0-indexed) element in Fibonacci sequence.

3. 0x10000010

4. First step through the 'addi' instructions on t0 and t1, then set the value of t0 to 3 and t1 to 5
